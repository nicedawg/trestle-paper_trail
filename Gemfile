source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in trestle-paper_trail.gemspec.
gemspec

group :test do
  gem "capybara"
  gem "cuprite"
  gem "puma"
end

gem "paper_trail"
gem "sass-rails"
gem "sqlite3"
gem "trestle", github: "TrestleAdmin/trestle"

# Start debugger with binding.b [https://github.com/ruby/debug]
# gem "debug", ">= 1.0.0"
